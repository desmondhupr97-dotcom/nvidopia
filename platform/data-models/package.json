{
  "name": "@nvidopia/data-models",
  "version": "0.1.0",
  "description": "Shared Mongoose data models for the nvidopia autonomous driving test platform",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "prepublishOnly": "npm run build",
    "test": "vitest run"
  },
  "dependencies": {
    "mongoose": "^8.10.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "rimraf": "^6.0.0"
  },
  "license": "UNLICENSED",
  "private": true
}
